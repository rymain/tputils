#!/bin/bash   
MACHINE=$1

if [ "$MACHINE" == "einstein" ]; then
	ssh -i "~/secret/aws/aws-key.pem" ubuntu@ec2-13-234-184-74.ap-south-1.compute.amazonaws.com
else
	echo "Server '$MACHINE' not known"
fi
